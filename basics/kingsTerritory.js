
//let king= 'john';

if(true)
{
	//let king='sam';
	if(true)
	{
		king='ram'
		console.log(king);
	}
}
saikat=0;

/*when you are declaring some variable without var or let
then also no error will come but in that case that variable 
will be created as a global
so try to use always var or let while declaring variables*/
if(true)
{
	console.log("I am 2nd king :"+king);
}